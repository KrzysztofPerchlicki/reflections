What happens when you initialize a repository? Why do you need to do it?

Git environment is created without commits. (.git folder is created and master branch). You need it to make commits and have all git stuff. 
